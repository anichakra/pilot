server:
  port: ${PORT:8080}
spring: 
  h2: 
    console:
      enabled: true
      path: /h2-console
  profiles:
    active: test
rule:
  template-path: ${RULE_TEMPLATE_PATH:vehicle-rules.xls}


api:
  context-path: /@project.name@/@project.version@