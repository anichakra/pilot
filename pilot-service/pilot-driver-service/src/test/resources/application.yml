server:
  port: ${PORT:8081}
spring: 
  h2: 
    console:
      enabled: true
      path: /h2-console
  profiles:
    active: test

# Rest consumer calls

api:
  context-path: /pilot-driver-service/0.0.1.BUILD-SNAPSHOT
 